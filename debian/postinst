#!/bin/sh
set -e
# Refresh the GTK icon cache so the app icon appears in menus immediately
if which gtk-update-icon-cache >/dev/null 2>&1; then
    gtk-update-icon-cache /usr/share/icons/hicolor/ || true
fi
